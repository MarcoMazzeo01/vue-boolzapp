const {createApp} = Vue 

const Boolzapp = createApp({
    data() {
        return {
            contacts: contacts,
        }

    },

    method() {
        
    },

    mounted() {
        console.log(this.contacts)
    }
}).mount("#boolzapp")